import React from 'react'

const CharacterItem = ({item}) => {
    return (
        <div>
            {item.name}
        </div>
    )
}

// CharacterItem.propTypes = {

// }

export default CharacterItem
